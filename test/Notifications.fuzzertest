var fuzzedData=[];
async function performFuzzTest(iterations) {
    try {
        for (let i = 0; i < iterations; i++) {

            const status = Math.random()*1000 %3;
            const InvoiceNumber = Math.floor(Math.random()*100000000);
            if(status==1){//due in 1 day
                fuzzedData.push("Invoice "+InvoiceNumber+" is due in 1 day");
            }else if(status==2){//due in 3 days
                fuzzedData.push("Invoice "+InvoiceNumber+" is due in 3 days");
            }else{
                fuzzedData.push("Invoice "+invoice.invoiceid+" is overdue");
            };
        };
        return fuzzedData;
    };
};


const numIterations = 10;
performFuzzTest(numIterations);
console.log(dropdownItems);
